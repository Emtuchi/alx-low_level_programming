#include <stdio.h>
/**
 * main - Entry point for code
 * Return: 0 (success)
 */
int main(void)
{
	char lc;

	lc = 'a';
	while
		(lc <= 'z') {
			if ((lc != 'q' && lc != 'e') && c <= 'z')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
